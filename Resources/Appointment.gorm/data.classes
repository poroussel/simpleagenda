{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppointmentEditor = {
	Actions = (
	    "cancel:",
	    "validate:",
	    "selectFrequency:"
	);
	Outlets = (
	    description,
	    title,
	    duration,
	    repeat,
	    endDate,
	    durationText,
	    location,
	    store,
	    allDay,
	    ok
	);
	Super = NSWindowController;
    };
    FirstResponder = {
	Actions = (
	    "selectFrequency:",
	    "validate:"
	);
	Super = NSObject;
    };
}